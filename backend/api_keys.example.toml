# 复制为 backend/api_keys.toml 并替换 key（该文件不入库）
# scopes: read（全部 GET）/ write（创建、删除自己的任务）/ admin（reindex、删除任意任务、查看全部任务）

[[keys]]
id = "frontend-web"
key = "yaoe_replace_me_frontend"
scopes = ["read", "write"]

[[keys]]
id = "ops"
key = "yaoe_replace_me_ops"
scopes = ["read", "write", "admin"]
