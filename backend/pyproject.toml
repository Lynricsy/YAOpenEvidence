[project]
name = "yaoe-backend"
version = "0.1.0"
description = "YAOpenEvidence REST/SSE 后端（FastAPI + arq worker）"
requires-python = ">=3.12,<3.13"
dependencies = [
  "picosgpt-core[embed,paywall]",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.7",
  "pydantic-settings>=2.7",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
  "arq>=0.26",
  "redis>=5",
  "sse-starlette>=2.1",
  "argon2-cffi>=25.1",
  "python-multipart>=0.0.20",
  # codex 引擎：SDK 依赖 openai-codex-cli-bin，二进制随 wheel 进镜像，容器内无需 node
  "openai-codex>=0.147",
]

[project.scripts]
yaoe = "app.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
