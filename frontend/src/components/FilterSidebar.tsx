import { useState } from 'react'
import { RotateCcw, SlidersHorizontal, ChevronDown, Info } from 'lucide-react'
import { DEFAULT_FILTERS, describeFilters, type FilterState } from '@/lib/filters'
import { YearPicker } from './YearPicker'
import { JournalPicker } from './JournalPicker'
import { Button } from './ui/button'
import { Checkbox } from './ui/checkbox'
import { Switch } from './ui/switch'
import { Slider } from './ui/slider'
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from './ui/select'
import { Collapsible, CollapsibleTrigger, CollapsibleContent } from './ui/collapsible'
import { Tooltip, TooltipTrigger, TooltipContent } from './ui/tooltip'
import { Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle } from './ui/sheet'
function FilterContent({ value: f, onChange }: { value: FilterState; onChange: (value: FilterState) => void }) {
  const update = (patch: Partial<FilterState>) => onChange({ ...f, ...patch })
  return <div className="flex h-full min-h-0 flex-col"><div className="flex items-center justify-between border-b px-5 py-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><SlidersHorizontal className="size-4 text-primary" />文献筛选</h2><Button variant="ghost" size="icon-sm" aria-label="重置筛选" title="重置筛选" onClick={() => onChange({ ...DEFAULT_FILTERS })}><RotateCcw className="size-3.5" /></Button></div><div className="min-h-0 flex-1 overflow-y-auto px-5"><section className="space-y-4 border-b py-5"><h3 className="text-xs font-semibold text-muted-foreground">期刊分区</h3><div className="space-y-3">{[1,2,3,4].map(q => <label key={q} className="flex cursor-pointer items-center gap-3"><Checkbox aria-label={q + '区 Q' + q} checked={f.quartiles.includes(q)} onCheckedChange={checked => update({ quartiles: checked ? [...f.quartiles,q].sort() : f.quartiles.filter(v => v !== q) })} /><span className="size-1.5 rounded-full" style={{background:'var(--q' + q + ')'}} /><span className="text-sm">{q}区</span><span className="ml-auto font-mono text-xs text-muted-foreground">Q{q}</span></label>)}</div><label className={'flex items-center justify-between gap-2 text-xs ' + (!f.quartiles.length ? 'text-muted-foreground' : '')}>含未收录期刊<Switch aria-label="含未收录期刊" checked={f.keepUnranked} disabled={!f.quartiles.length} onCheckedChange={v => update({keepUnranked:v})} /></label></section><section className="space-y-4 border-b py-5"><h3 className="text-xs font-semibold text-muted-foreground">年份</h3><YearPicker value={f} onChange={update} /></section><section className="space-y-4 border-b py-5"><h3 className="flex items-center justify-between text-xs font-semibold text-muted-foreground">期刊家族（含子刊）<Tooltip><TooltipTrigger asChild><button aria-label="期刊家族匹配规则"><Info className="size-3.5" /></button></TooltipTrigger><TooltipContent className="max-w-64">按期刊名子串匹配，如 Nature 同时命中 Nature Medicine 等子刊</TooltipContent></Tooltip></h3><JournalPicker value={f.journals} onChange={journals => update({journals})} /></section><Collapsible className="py-5"><CollapsibleTrigger className="flex w-full items-center justify-between text-xs font-semibold text-muted-foreground">高级<ChevronDown className="size-4" /></CollapsibleTrigger><CollapsibleContent className="space-y-6 pt-5"><div className="space-y-4"><div className="flex items-center justify-between text-xs">阅读篇数<span className="font-mono text-primary">{f.papers} 篇</span></div><Slider aria-label="阅读篇数" min={1} max={30} step={1} value={[f.papers]} onValueChange={([papers]) => update({papers})} /></div><label className="flex items-center justify-between gap-2 text-xs">写入并使用知识库<Switch aria-label="写入并使用知识库" checked={f.useKb} onCheckedChange={useKb => update({useKb})} /></label><div className="space-y-4"><div className="flex justify-between text-xs">附加知识库命中<span className="font-mono">{f.kbHits}</span></div><Slider aria-label="附加知识库命中" min={0} max={20} step={1} value={[f.kbHits]} disabled={!f.useKb} onValueChange={([kbHits]) => update({kbHits})} /></div><div className="space-y-2"><span className="text-xs">单篇字符预算</span><Select value={String(f.maxChars)} onValueChange={v => update({maxChars:Number(v)})}><SelectTrigger aria-label="单篇字符预算" className="w-full"><SelectValue /></SelectTrigger><SelectContent>{[12000,20000,28000,40000,60000].map(n => <SelectItem value={String(n)} key={n}>{n.toLocaleString()} 字符</SelectItem>)}</SelectContent></Select></div></CollapsibleContent></Collapsible></div><p className="border-t bg-muted/50 px-5 py-4 text-xs leading-6 text-muted-foreground" data-testid="filter-summary">{describeFilters(f)}</p></div>
}
export function FilterSidebar(props: { value: FilterState; onChange: (value: FilterState) => void }) {
  const [open, setOpen] = useState(false)
  return <><aside className="hidden w-72 shrink-0 border-r bg-card/60 lg:block"><FilterContent {...props} /></aside><div className="absolute left-4 top-3 z-10 lg:hidden"><Sheet open={open} onOpenChange={setOpen}><SheetTrigger asChild><Button variant="outline" size="sm"><SlidersHorizontal />筛选</Button></SheetTrigger><SheetContent side="left" className="w-80 gap-0"><SheetHeader className="sr-only"><SheetTitle>文献筛选</SheetTitle></SheetHeader><FilterContent {...props} /></SheetContent></Sheet></div></>
}
