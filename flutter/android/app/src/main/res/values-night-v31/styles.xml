<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 深色 + Android 12+：night 与版本限定符都更精确，会盖掉
         values-night/styles.xml 与 values-v31/styles.xml。 -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/brand_canvas</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/brand_splash_dark</item>
    </style>
</resources>
